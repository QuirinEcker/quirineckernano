function scrollListener() {
    let scrollPosition = scrollY;
    let scrollCounter = document.getElementById("scrollCounter");
    scrollCounter.innerHTML = scrollPosition;

    if(scrollPosition > 684) {
    }
}